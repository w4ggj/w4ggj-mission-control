# ── secrets / local-only config (NEVER commit) ──
agent.config.json
# your LIVE station config — edit freely; 'git pull' won't fight it.
# The tracked template is station.config.example.json (copy it to start).
station.config.json
*.bak
*.env
.env

# ── runtime data (visitor analytics store, regenerated at runtime) ──
analytics_data.json
analytics_data.tmp

# ── TLS certs ──
*.pem

# ── python ──
__pycache__/
*.pyc
*.pyo

# ── editor / os ──
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── local claude settings ──
.claude/settings.local.json
